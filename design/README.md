# Thư mục design

Chứa file gốc phục vụ thiết kế, **không** được deploy lên server (khác với `public/`).

- `logo-english-study.png` — ảnh logo gốc 1600×1590. Dùng để tạo lại các icon
  trong `public/icons/` (`favicon-32.png`, `icon-192.png`, `icon-512.png`) khi cần.
